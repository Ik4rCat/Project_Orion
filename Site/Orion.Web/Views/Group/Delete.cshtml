@model Orion.Core.Models.Group

@{
    ViewData["Title"] = "Удаление группы";
}

<div class="container">
    <div class="card mt-4">
        <div class="card-header bg-danger text-white">
            <h5 class="mb-0">Подтверждение удаления</h5>
        </div>
        <div class="card-body">
            <h4>Вы действительно хотите удалить группу "@Model.Name"?</h4>
            
            <div class="alert alert-warning mt-3">
                <i class="bi bi-exclamation-triangle me-2"></i>
                <strong>Внимание!</strong> Это действие нельзя будет отменить. Все данные группы, включая связи с пользователями, будут удалены.
            </div>
            
            <div class="mt-4">
                <form asp-action="Delete" method="post">
                    <input type="hidden" name="id" value="@Model.Id" />
                    <button type="submit" class="btn btn-danger">Удалить</button>
                    <a asp-action="Index" class="btn btn-outline-secondary ms-2">Отмена</a>
                </form>
            </div>
        </div>
    </div>
</div> 